{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev",
    "build": "lerna run build"
  },
  "dependencies": {
    "@module-federation/runtime": "^2.0.1",
    "@module-federation/sdk": "^2.0.1"
  },
  "devDependencies": {
    "lerna": "^9.0.0"
  }
}
